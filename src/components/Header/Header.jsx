// Component that builds the header and the contained
// links that may appear
export default function Header() {

    // Build the DOM elements
    return (
        <header>
            <h1>The Movies Saga!</h1>
        </header>
    )
}